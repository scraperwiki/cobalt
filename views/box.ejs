{
  "status": "ok",
  "message": [
    "This is a box that you can SSH to.",
    "Add your SSH public key with the web API.",
    "curl --data-urlencode sshkey@$(printf ~/.ssh/id_[rd]sa.pub) --data apikey=<apikey> <%= rooturl %>/<boxname>/sshkeys",
    "Then connect.",
    "ssh <%= box_name %>@<%= server_ip %>"
  ]
}
